<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kerris.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".ServiceActivity" />
        <activity android:name=".ProviderActivity" />
        <activity android:name=".ProviderChooseActivity" />
        <activity android:name=".SPServiceActivity" />
        <activity android:name=".SPTimeActivity" />
        <activity android:name=".HomeownerActivity" />
        <activity android:name=".HoSearchActivity" />
        <activity android:name=".HoViewActivity" />
        <activity android:name=".HORateActivity"></activity>
    </application>

</manifest>